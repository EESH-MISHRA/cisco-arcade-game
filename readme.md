# Cisco Arcade

## Prerequisites
* **Python Version:** Python 3.8 or higher (Versions 3.8 through 3.11 are highly recommended for optimal compatibility with the MediaPipe library).

## Setup and Installation

1. **Open your terminal or command prompt.**
2. **Install dependencies:** Install all the necessary libraries by running the following command in the directory containing your `requirements.txt` file:
   ```bash
   pip install -r requirements.txt
